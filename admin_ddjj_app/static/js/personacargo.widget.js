(function($){
/*
    $(function() {
        function filterPersonaCargo() {
            var persona_id = $('#id_persona').val();
            console.info(persona_id);
            if(!!persona_id) {
                $('#id_persona_cargo option').hide();
                var personas = $('#id_persona_cargo option[data-persona="'+ persona_id + '"]').show();
                console.info(personas.length);
            }
        }
        $('#id_persona').on('change', function() {
            $('#id_persona_cargo').val([]);
            filterPersonaCargo();
        });

        $("#id_persona").on("focus", function() {
            $('#id_persona_cargo').val([]);
            filterPersonaCargo();
        });

        filterPersonaCargo();
    });
    */
})(grp.jQuery);
